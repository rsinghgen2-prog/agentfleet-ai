# Leave blank for local `npm run dev` — Vite proxies /api/v1/auth and /api/v1/patients.
# Set these only for a production build that talks to the APIs on another origin.
VITE_API_URL=
VITE_AUTH_API_URL=
VITE_DEMO_MODE=false
VITE_DEMO_VPS_PASSWORD=
VITE_DEMO_ABC_PASSWORD=
VITE_SUPER_ADMIN_PASSWORD=